#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]){
    char *text = "20";
    int text_to_i = atoi(text);
    printf("Text: %s\n", text);
    printf("Text to integer: %d\n", text_to_i);
    return 0;
}
